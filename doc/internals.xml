<Chapter Label="chap:GlobalVars"><Heading>Global Variables</Heading>

 <Section Label="chap:InfoClass"><Heading>Getting information about polymake
output</Heading> 
   
    <ManSection>
     <InfoClass Name="InfoPolymaking" />
    <Description>
     If set to at least <M>2</M>, the output of polymake is shown.
     At level <M>1</M>, warnings are shown. This is the default.
     And at level <M>0</M>, the polymake package remains silent.
    </Description>
   </ManSection>

    <ManSection>
     <Var Name="POLYMAKE_LAST_FAIL_REASON"/>
    <Description>
     Contains a string that explains the last occurrence of <K>fail</K> as a
     return value of <Ref Meth="Polymake"/>.
    </Description>
    </ManSection>
   </Section>


 <Section Label="variables">
  <Heading>Variables for system interaction</Heading>

  Up to version 0.8.9 the polymake command and the directory for polymake
  files were held in the global variables <K>POLYMAKE&uscore;COMMAND</K> and
  <K>POLYMAKE&uscore;DATA&uscore;DIR</K>. These are deprecated; they are still
  honoured if you bind them yourself, but the package no longer sets them and
  warns at load time if it finds them. Use the user preferences
  <C>PolymakeCommand</C> and <C>PolymakeDataDirectory</C> instead, see
  <Ref Sect="chap:varsetters"/>, and <Ref Func="PolymakeCommand"/> and
  <Ref Func="PolymakeDataDirectory"/> to query the effective values.

</Section>

</Chapter>



 <Chapter Label="Converting"><Heading>Converting Polymake Output</Heading>

<Section><Heading>The General Method</Heading>

polymaking hands its requests to the polymake script <F>lib/pm.pl</F>, which
evaluates each of them and serializes the results into a single JSON file using
polymake's own serializer. &GAP; reads that file with the
<Package>json</Package> package and converts each value.

<P/>

The conversion is driven by the polymake type of the value, which is part of
what polymake writes, so there is no table mapping keywords to parsers -- a
<C>Matrix&lt;Rational&gt;</C> is decoded as a matrix of rationals whether or not
polymaking has ever heard of the keyword. Note that polymake writes its own
number types as strings, so that no precision is lost.

<Subsection><Heading>Converter Philosophy</Heading>

The converters take meaningful polymake data into meaningful
&GAP; data. This sometimes means that the (mathematical) representation is
changed. Here is an example: polymake writes vectors as augmented affine
vectors of the form  <Code>1 a1 a2 a3...</Code> which does not go very well
with the usual &GAP; conventions of column vectors and multiplying matrices
from the right. So <Package>polymaking</Package> converts such a vector to
<Code>[a1,a2,a3,...]</Code> and the user is left with the problem of
augmentation and left or right multiplication. This applies to
<C>POINTS</C>, <C>VERTICES</C>, <C>REL&uscore;INT&uscore;POINT</C>,
<C>VALID&uscore;POINT</C> and <C>VERTEX&uscore;BARYCENTER</C>; it is a property
of the keyword, not of the type, so it cannot be derived.
<P/>
Another area where the &GAP; object isn't a literal translation from the
polymake world is combinatorics. In polymake, list elements are enumerated
starting from 0. &GAP; enumerates lists starting at 1. So the conversion
process adds 1 to everything that is an index: sets of integers, incidence
matrices and graph adjacency lists. Lists of plain integers, such as
<C>FACET&uscore;DEGREES</C>, are values rather than indices and are left alone.
<P/>
</Subsection>

The conversion is done by the following functions. You are unlikely to need
them directly.

  <ManSection>
  <Func Name="PolymakeParseType" Arg="string"/>
   <Returns>Record with components <C>name</C> and <C>params</C></Returns>
   <Description>
    Parses a polymake type such as <C>"common::Array&lt;Set&lt;Int&gt;&gt;"</C>
    into a record; the application prefix is dropped and type parameters are
    parsed recursively. Passing <K>fail</K>, which is how the
    <Package>json</Package> package reports a JSON <C>null</C>, gives the empty
    name; polymake uses <C>null</C> for values that are plain Perl scalars.
   </Description>
  </ManSection>

  <ManSection>
  <Func Name="PolymakeDecodeValue" Arg="type data"/>
   <Description>
    Converts the JSON value <A>data</A> into a &GAP; object, guided by the
    parsed <A>type</A>. Understands scalars, dense and sparse vectors and
    matrices, sets, incidence matrices, graph adjacency lists, node maps and
    arrays. Unknown types are returned unchanged, with a warning at
    <Ref InfoClass="InfoPolymaking"/> level 1.
   </Description>
  </ManSection>

  <ManSection>
  <Func Name="PolymakeDecodeProperty" Arg="keyword entry"/>
   <Description>
    Calls <Ref Func="PolymakeDecodeValue"/> for the polymake result
    <A>entry</A> and then applies the conventions that depend on
    <A>keyword</A> rather than on the type, as described above.
   </Description>
  </ManSection>

  <ManSection>
  <Func Name="PolymakeEncodeObject" Arg="type properties"/>
   <Returns>String</Returns>
   <Description>
    The other direction: renders the record <A>properties</A> as a polymake
    data file of polymake type <A>type</A>. Rational numbers are written as
    strings, which is how polymake spells them, so nothing is rounded.
   </Description>
  </ManSection>

</Section>

</Chapter>
